# Clear previous convnos

This repo includes a helper script to clear previous Cursor conversation/session
folders on the current machine.

## Run it

```bash
chmod +x ./clear_convnos.sh
./clear_convnos.sh
```

## Preview only (no delete)

```bash
./clear_convnos.sh --dry-run
```
